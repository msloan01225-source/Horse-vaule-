import pandas as pd

def get_usa_data(day="Today", use_proxies=False, debug=False):
    """
    Placeholder USA scraper for BetEdge.
    Currently returns an empty DataFrame so the app can run without errors.
    """
    print(f"[USA] ⚠ Placeholder scraper called for {day}")
    return pd.DataFrame()
