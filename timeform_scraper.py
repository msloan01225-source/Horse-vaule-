import pandas as pd

def get_timeform_data(day="Today", use_proxies=False, debug=False):
    """
    Placeholder Timeform scraper for BetEdge.
    Matches the expected function signature in app.py so it won't crash.
    Currently returns an empty DataFrame.
    """
    print(f"[Timeform] ⚠ Placeholder scraper called for {day} (proxies={use_proxies}, debug={debug})")
    return pd.DataFrame()
