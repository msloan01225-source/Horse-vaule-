import pandas as pd

def get_timeform_data(day="Today", use_proxies=False, debug=False):
    """
    Placeholder Timeform scraper for BetEdge.
    Currently returns an empty DataFrame so the app can run without errors.
    """
    print(f"[Timeform] ⚠ Placeholder scraper called for {day}")
    return pd.DataFrame()
